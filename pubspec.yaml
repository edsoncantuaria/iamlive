name: estou_vivo
description: "Estou Vivo! — App de segurança para quem mora sozinho."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.0
  url_launcher: ^6.2.0
  flutter_local_notifications: ^17.0.0
  provider: ^6.1.0
  google_fonts: ^6.1.0
  intl: ^0.19.0
  workmanager: ^0.5.0
  timezone: ^0.9.0
  whatsapp_bot_flutter_mobile: ^2.1.2
  qr_flutter: ^4.1.0
  flutter_inappwebview: ^6.1.5
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
